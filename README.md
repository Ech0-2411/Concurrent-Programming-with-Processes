# Concurrent-Programming-with-Processes
This project is an implementation of a concurrent programming framework something like the "map-reduce" paradigm, using concurrent processes to count occurrences of words in (text) files.  The general framework involves three different types of processes:
